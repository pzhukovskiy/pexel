name: actions

on:
  push:
    branches: 
      - main
  pull_request:
    branches: 
      - main

jobs:
  build_job:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2
        - name: Run script
          run: echo "Hello, World from Pavel"
          shell: bash
